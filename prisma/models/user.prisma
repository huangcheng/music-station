model User {
  id          Int      @id @default(autoincrement())
  email       String?  @unique
  name        String   @unique
  displayName String?
  password    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  sessions Session[]
}
